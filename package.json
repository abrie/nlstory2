{
  "name": "nlstory2",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "package-deployment": "yarn build && mv builds/ dist/",
    "generate-summary": "python scripts/generate_summary.py --build-significant-steps"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^5.4.11"
  }
}
